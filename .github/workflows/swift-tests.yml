name: Swift Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      
      - name: Accept Xcode license
        run: sudo xcodebuild -license accept
      
      - name: Build and Test
        run: xcodebuild -project Clam.xcodeproj -scheme Clam -destination 'platform=iOS Simulator,name=iPhone 12,OS=15.0' test
