workflows:
  ci-workflow:
    name: Pipeline
   scripts:
    - name: iOS test
      script: |
        xcode-project run-tests \
          --workspace Clam.xcodeproj \
          --scheme Clam \
          --device "iPhone 11"
    test_report: build/ios/test/*.xml
   
pulishing:
   email:
   recipients:
    -sagarstpss@gmail.com
    

